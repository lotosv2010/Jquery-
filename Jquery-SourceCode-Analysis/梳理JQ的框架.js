(function() {
    // (21, 94) 定义了一些变量和函数 jQuery = function(){}

    // (96, 283) 给JQ对象，添加一些方法和属性

    // (258, 347) extend:JQ的继承方法

    // (349, 817) jQuery.extend(): 扩展一些工具方法

    // (8826) window.jQuery = window.$ = jQuery
})()